import java.time.LocalDateTime;
import java.util.Scanner;

public class Semi_1 {
    public static void main(String[] args) throws Exception {
        // Scanner scan = new Scanner(System.in);
        // String name = scan.nextLine();
        // int time = LocalDateTime.now().getHour();
        // System.out.println(time);
        // if (time >= 5 && time < 12) {
        // System.out.printf("Доброе утро, " + name + "!");
        // }
        // if (time >= 12 && time < 18) {
        // System.out.printf("Добрый день, " + name + "!");
        // }
        // if (time >= 18 && time < 23) {
        // System.out.printf("Добрый вечер, " + name + "!");
        // }
        // if (time >= 23 && time <= 24 || time >= 0 && time < 5) {
        // System.out.printf("Доброй ночи, " + name + "!");
        // }
        // scan.close();

        // int[] a = new int[] { 1, 1, 1, 0, 0, 1, 0, 1 };
        // int count = 0;
        // int max_count = 0;
        // for (int i = 0; i < a.length; i++) {
        // if (a[i] == 1) {
        // count++;
        // }
        // else {
        // if (max_count < count){
        // max_count = count;
        // };
        // count = 0;
        // }

        // }
        // System.out.println(max_count);
    }
    public static void swap(int[] A, int i, int j){
        int temp = A[i];
        A[i] = A[j];
        A[j] = temp;
    }
    public static void patition(int[] A){
        int j = 0;
    }
}
